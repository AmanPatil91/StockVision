{
  "name": "stockvision",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @stockvision/web",
    "build:web": "npm run build --workspace @stockvision/web",
    "lint:web": "npm run lint --workspace @stockvision/web",
    "typecheck:web": "npm run typecheck --workspace @stockvision/web"
  }
}
